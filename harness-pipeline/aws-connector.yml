connector:
  name: aws-connector
  identifier: aws-connector
  description: "AWS connector for React app deployment to S3"
  accountIdentifier: <+account.identifier>
  orgIdentifier: <+org.identifier>
  projectIdentifier: <+project.identifier>
  type: Aws
  spec:
    credential:
      type: ManualConfig
      spec:
        accessKey: <+secrets.getValue("aws_access_key")>
        secretKeyRef: <+secrets.getValue("aws_secret_key")>
        sessionTokenRef: <+secrets.getValue("aws_session_token")>
      region: us-east-1
    awsSdkClientBackOffStrategyOverride:
      type: EqualJitterBackoffStrategy
      spec:
        baseDelay: 1
        maxBackoffTime: 1
        retryCount: 1
    executeOnDelegate: false
    proxy: false
    ignoreTestConnection: false
